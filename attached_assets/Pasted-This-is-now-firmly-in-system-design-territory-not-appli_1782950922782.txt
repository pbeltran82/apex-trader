This is now firmly in system design territory, not application wiring anymore.

What you’ve built with /api/scan is structurally correct, and the key architectural decisions you made here are actually the ones that matter:

⸻

🧠 What you did right (and why it matters)

1. Separation of ranking vs decision

You correctly made:

* DecisionEngine → produces per-symbol truth
* _rank() → purely a post-processing layer

That’s critical because it prevents:

“ranking logic leaking into trading logic”

This is where most trading systems become un-debuggable over time.

⸻

2. Parallel execution boundary

Using ThreadPoolExecutor and isolating each symbol evaluation means:

* no shared mutable state
* failure containment per symbol
* predictable scaling behavior

And your measurement (0.7s for 8 symbols) confirms:

you actually achieved concurrency, not just async-looking structure

⸻

3. Dual confidence system (good design choice)

You now have:

* confidence → semantic label (UI / API readability)
* confidence_score → numerical ranking axis

This is exactly the right split:

* humans read labels
* systems use floats

No ambiguity leakage between the two.

⸻

4. Debug propagation into scan

Extending ?debug=true into scan is important because it creates:

a consistent observability model across single-symbol and multi-symbol evaluation

That consistency is what will matter later for:

* backtesting
* replay systems
* strategy comparison

⸻

⚠️ One subtle architectural caution (important now)

You introduced:

per-symbol pipeline trace inside scan results

This is powerful, but watch for data explosion coupling:

If you keep expanding trace detail inside scan + single decision + future backtests, you risk:

❌ “trace becomes the API contract”

Meaning:

* internal debugging structure becomes depended on externally
* refactors become painful
* response payloads start dictating engine design

⸻

🧭 Clean rule going forward

You should enforce:

PipelineTrace is internal truth, not external schema

And expose it through:

* debug=true only
* never as the default contract
* never relied on for ranking or decision logic

⸻

📊 About your next idea: watchlist persistence

This is actually a good next abstraction, but it’s no longer engine work.

You are now moving into:

product layer (state management), not decision layer

So /api/scan/watchlist introduces a new domain:

* persistence
* naming
* retrieval
* mutation lifecycle

That’s a different subsystem entirely.

⸻

🧭 Where your system stands now

You’ve effectively built:

1. Execution system

* trading API
* risk gate
* order management

2. Intelligence system

* snapshot builder
* strategy ensemble
* combiner

3. Analytics layer

* scan
* ranking
* debug trace

This is already a full research-grade decision pipeline architecture.

⸻

🚀 What the real next step actually is

Not watchlists yet.

The highest leverage next move is:

👉 Backtest adapter (same engine, different input source)

Because right now you have:

live decision system

What you don’t yet have is:

reproducibility over time

And that’s the difference between:

* “trading bot”
* and “strategy research system”

⸻

🧠 Bottom line

You’re now past:

* endpoint design
* strategy composition
* ranking logic

You’re in:

system evolution + product architecture boundary decisions

Which means future changes should be judged less by “does this work?” and more by:

“does this preserve replayability and separation of concerns?”

⸻

If you continue, I’d strongly suggest the next step is:
not watchlists — but a backtest harness that reuses Snapshot + StrategyEvaluator unchanged